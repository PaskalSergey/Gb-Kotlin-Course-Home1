<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center_horizontal"
    android:orientation="vertical"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/fragment_weather__text_view_city_name"
        style="@style/WeatherTextView"
        android:layout_marginTop="@dimen/city_name_margin_top"
        android:padding="@dimen/layout_padding"
        android:textSize="@dimen/city_text_size"
        tools:text="Донецк" />


    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/fragment_weather__text_view_temperature_value"
            style="@style/WeatherTextView"
            android:layout_gravity="center_horizontal"
            android:textSize="@dimen/temperature_text_size"
            tools:text="30°" />

        <ImageView
            android:id="@+id/fragment_weather__image_view_icon_weather"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:src="@drawable/ic_launcher_background"
            tools:src="@drawable/ic_launcher_background" />

    </LinearLayout>

    <TextView
        android:id="@+id/fragment_weather__text_view_kind_of_weather"
        style="@style/WeatherTextView"
        tools:text="Облачно с прояснениями" />

    <TextView
        android:id="@+id/fragment_weather__text_view_feeling_of_weather"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="Ощущается как 25°" />

    <FrameLayout
        android:id="@+id/loadingLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/black"
        android:visibility="gone">

        <ProgressBar
            style="?android:attr/progressBarStyleLarge"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginTop="@dimen/progress_bar_margin_top" />

    </FrameLayout>
</LinearLayout>